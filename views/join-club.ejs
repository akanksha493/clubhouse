<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Join Club</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>
<body>
    <% if(user && !user.isMember){ %>
        <form action="" method="POST" class="m-4">
            <label for="scode" class="form-label">SecretCode</label>
            <input type="text" name="scode" id="scode" class="form-control my-2">

            <button class="btn btn-primary" type="submit">Join</button>
        </form>
    <% }else if(user && user.isMember){ %>
        <p>YOU ARE ALREADY A MEMBER! WHAT ARE YOU DOING BRUH?!</p>
    <% }else{ %>
        <p>you are not allowed here! Login first</p>
    <% } %>
</body>
</html>